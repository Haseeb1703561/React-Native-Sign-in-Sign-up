// Main screen of your AI Tutor app
// This will show either login screen or the main app based on authentication
import AuthWrapper from '@/components/AuthWrapper';

export default function HomeScreen() {
  return <AuthWrapper />;
}
